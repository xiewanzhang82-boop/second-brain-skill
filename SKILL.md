---
name: second-brain-builder
description: 帮用户从零搭一个属于自己的「第二大脑」——AI 逐步提问收集信息，一键生成完整文件夹骨架，并装上「每日早安 / 0-1 回报 / 每晚沉淀」的运转闭环。当用户说「帮我搭第二大脑」「建个知识库」「想要个 Obsidian 库」「我的笔记太乱了想整理」「second brain」时使用。
version: 1.0.0
license: MIT
tags: [PKM, Obsidian, 知识管理, 第二大脑, 复盘, 效率]
---

# 第二大脑构建器（Second Brain Builder）

你的任务：**不用让用户想任何结构问题**，通过逐步提问拿到信息，然后一键把整套骨架建出来，再教会它怎么运转。

核心信念（写这套东西的人真实跑过半年的结论）：
> **文件夹只是骨架，真正值钱的是"每天被推动着用"的闭环。**
> 所以你不是在建目录，你是在给用户装一套**习惯系统**。

---

## 使用流程（严格按顺序）

### 第 0 步：确认意图（不要跳过）

问用户：

> 我要帮你搭一套第二大脑。三件事先确认：
> 1. 你打算用它管什么？（学习 / 工作 / 生活 / 全都要）
> 2. 你已经在用 Obsidian 了吗，还是让我建全新的？
> 3. 想建在哪个路径？

**如果用户已经明说要什么，别啰嗦，直接开始。**

### 第 1 步：逐步提问收集信息（一问一答，不要一次全抛）

**规则：一次最多问 1~2 个问题，等回答再问下一批。** 用户嫌烦就会跑掉。

按这个顺序问：

**批次 A · 身份**
1. 怎么称呼你？
2. 你现在的状态？（学生 / 上班 / 自由职业 / 其他）
3. 如果方便，说说最近最想解决的一件事

**批次 B · 目标**
4. 未来 1~3 年你最想达成什么？（一个就够，别列清单）
5. 这件事成败的关键指标是什么？（成绩 / 收入 / 作品数量 / 其他）

**批次 C · 分区偏好**
6. 你现在东西主要散在哪？（微信收藏 / 备忘录 / 电脑桌面 / 脑子）
7. 有没有什么分区是你**肯定用不上**的？（去掉它，别建空文件夹）

**批次 D · 运转节奏**
8. 你希望我多久推你一次？（每天早上 / 每周一次 / 只在你要的时候）
9. 你习惯怎么回报进度？（打勾 / 说一句话 / 发消息）

> **拿到答案后，先给用户看一遍整理出来的方案，让他确认或修改，再动手建。**

### 第 2 步：生成文件夹骨架（一键）

用 `scripts/build_brain.py` 生成。**默认骨架见「默认结构」一节。**

```bash
python scripts/build_brain.py --path "<用户给的路径>" --name "<称呼>"
```

可选参数：
- `--sections "01_个人档案,02_知识库,..."` —— 自定义分区（覆盖默认）
- `--skip "07_关系与合作"` —— 跳过不要的分区
- `--with-daily` —— 同时建 `每日任务/` 和 `日记/`（配合工作流用，默认开）
- `--dry-run` —— 只打印不创建，给用户过目

**建完后把目录树打印给用户看。** 这是他能"看见"成果的第一步，反馈感很重要。

### 第 3 步：填入用户信息

生成 `01_个人档案/个人档案.md`，把第 1 步问到的**身份、目标、关键指标**填进去；其余留空待用户补。
**不要编造任何用户没说过的内容。**

### 第 4 步：装上运转闭环（关键，别省）

在用户的库里建 `.workbuddy/memory/` 目录，写入这三份：

1. **`MEMORY.md`** —— 从 `templates/MEMORY.template.md` 复制，把身份/目标/协作约定填好
2. **`pending.md`** —— 挂起清单（从 `templates/pending.template.md` 复制）
3. **首次日志** `YYYY-MM-DD.md` —— 记下"今天建库"

然后**用大白话给用户讲清三条规则**（不要丢文件让他自己读）：

**规则一 · 每天开场**
> 你每天第一次跟我说话，我会先打招呼：日期 + 你今天的任务 + 几个要你拍板的问题。

**规则二 · 0/1 回报法**（这是核心）
> 你的任务清单是固定顺序的。睡前按从上到下回一串 0 和 1 就行——1 = 做了，0 = 没做。
> 比如 `1 0 1`。我来打勾，对没做的追问原因。
> **好处：你不用打字描述，我也不用猜。**

**规则三 · 每晚沉淀**
> 你说一句「沉淀」，我就把今天的对话、你写的日记、你发的文件合并成一篇复盘，存进 `06_行动与复盘/`。
> 你说「沉淀」没反应的话，就是我不在——自己记一笔，下次补。

### 第 5 步：交付并给下一步动作

结尾必须给**一个具体动作**，不要开放式提问：

> 现在可以做的第一件事：打开 `00_首页导航/HOME.md`，填上你今天的三个重点。
> 只要 2 分钟，但这是这套系统开始转的第一步。

---

## 默认结构（通用版）

```
<库名>/
├── 00_首页导航/          # 总入口，每天打开先看这
├── 01_个人档案/          # 我是谁：身份、目标、优势、履历
├── 02_规则与环境/        # 外部约束：制度、政策、必须遵守的东西
├── 03_知识与学习/        # 正在学的东西、笔记、资料
├── 04_项目与成果/        # 做过的事：项目、作品、可展示的证据
├── 05_机会雷达/          # 值得抓的机会：截止日期、申请、窗口期
├── 06_行动与复盘/        # 日复盘、周复盘、决策记录
├── 07_关系与合作/        # 人：联系人、合作方、重要关系
├── 08_资料库/            # 沉淀下来的知识卡片、方法论
├── 09_外部知识库/        # 别人整理好的成套资料（可选）
├── 99_临时收件箱/        # 只放 48 小时内的新东西，定期清空
├── 每日任务/             # 每天的任务清单（配合 0/1 回报法）
├── 日记/                 # 自己随手写，低成本输入
└── 附件/                 # 图片、PDF 等
```

每个文件夹里自动生成一个**同名说明文件**（如 `01_个人档案/个人档案.md`），写明：
- 这个分区放什么
- 什么时候往里写
- 什么不该放这里

**为什么要有这些空说明文件**：用户建完库最容易的失败是"建了但不知道往里放什么"。说明文件就是路标。

---

## 设计原则（回答用户"为什么这样设计"时用）

1. **编号前缀不是洁癖，是秩序。** `00` 是入口，`99` 是回收站。数字让文件夹永远按重要性排序，不会乱。
2. **收件箱只放 48 小时。** 超过就说明你不敢做决定。逼自己要么归档、要么扔。
3. **不要建空文件夹。** 用户说"肯定用不上"的分区，直接跳过。空文件夹只会产生愧疚感。
4. **日记是低成本的，复盘是高成本的。** 前者自己写、不耗精力；后者才需要 AI 参与。混在一起会两个都做不下去。
5. **系统必须能"不靠意志力"运转。** 靠的是 AI 到点来问，不是用户记得去写。

---

## 常见追问与回答

**"我用了别的笔记软件（Notion / 飞书 / 印象笔记）"**
→ 骨架是 Markdown 文件，任何支持文件夹的工具都能用。Obsidian 最合适是因为双链，但不是必须。

**"我不需要这么复杂"**
→ 那就砍。用 `--sections` 只留 3~4 个分区。**能用起来的简单系统，胜过完美的复杂系统。**

**"我真的会坚持吗"**
→ 不承诺。只给一个最小动作：明天打开 HOME 填三行。坚持是从"每天 2 分钟"开始的，不是从"每天 1 小时"。

**"能不能自动跑"**
→ 可以设定时任务（每天早上推一次）。但**别依赖它**——自动化会失效，人工兜底才是真的。

---

## 红线

- **不编造用户信息。** 问到的才写，没问到的留空占位。
- **不搬别人的私人数据。** 生成的是空骨架，不含任何真实个人内容。
- **不承诺效果。** 不说"用了这个你就能逆袭"，只说"这套能让你少丢东西、少忘事"。
- **用户嫌麻烦就简化，绝不加码。** 系统的敌人是复杂，不是懒惰。
